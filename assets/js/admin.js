document.addEventListener("DOMContentLoaded",function(){
  const gate=document.querySelector("[data-admin-gate]"),panel=document.querySelector("[data-admin-panel]");
  const form=document.querySelector("[data-settings-form]");
  function open(){gate.hidden=true;panel.hidden=false;load()}
  if(sessionStorage.getItem("queshift_admin")==="yes")open();
  document.querySelector("[data-admin-login]").addEventListener("submit",e=>{e.preventDefault();const fd=new FormData(e.target);if(String(fd.get("username")).toLowerCase()==="admin"&&fd.get("password")==="2026"){sessionStorage.setItem("queshift_admin","yes");open()}else document.querySelector("[data-login-error]").textContent="Incorrect login. Username: admin | Password: 2026"});
  document.querySelectorAll("[data-tab]").forEach(b=>b.addEventListener("click",()=>{document.querySelectorAll("[data-tab]").forEach(x=>x.classList.remove("active"));document.querySelectorAll(".admin-section").forEach(x=>x.classList.remove("active"));b.classList.add("active");document.getElementById(b.dataset.tab).classList.add("active")}));
  document.querySelector("[data-logout]").addEventListener("click",()=>{sessionStorage.removeItem("queshift_admin");location.reload()});
  function load(){const d=Queshift.data();form.heroTitle.value=d.heroTitle;form.heroText.value=d.heroText;form.phone1.value=d.phone1;form.phone2.value=d.phone2;form.email.value=d.email;renderTrusted(d);renderDownloads(d)}
  form.addEventListener("submit",e=>{e.preventDefault();const d=Queshift.data();d.heroTitle=form.heroTitle.value;d.heroText=form.heroText.value;d.phone1=form.phone1.value;d.phone2=form.phone2.value;d.email=form.email.value;Queshift.save(d);notice("Settings saved on this browser.")});
  document.querySelector("[data-add-trusted]").addEventListener("click",()=>{const input=document.querySelector("[data-trusted-name]");if(!input.value.trim())return;const d=Queshift.data();d.trusted.push(input.value.trim());Queshift.save(d);input.value="";renderTrusted(d)});
  function renderTrusted(d){document.querySelector("[data-admin-trusted]").innerHTML=d.trusted.map((x,i)=>`<article><span>${esc(x)}</span><button data-remove-trusted="${i}">Remove</button></article>`).join("");document.querySelectorAll("[data-remove-trusted]").forEach(b=>b.onclick=()=>{d.trusted.splice(+b.dataset.removeTrusted,1);Queshift.save(d);renderTrusted(d)})}
  const dlForm=document.querySelector("[data-download-form]");dlForm.addEventListener("submit",e=>{e.preventDefault();const fd=new FormData(dlForm),d=Queshift.data();d.downloads.push({title:fd.get("title"),version:fd.get("version"),details:fd.get("details"),url:fd.get("url")});Queshift.save(d);dlForm.reset();renderDownloads(d)});
  function renderDownloads(d){document.querySelector("[data-admin-downloads]").innerHTML=d.downloads.map((x,i)=>`<article><span><b>${esc(x.title)}</b><br><small>${esc(x.version)}</small></span><button data-remove-download="${i}">Remove</button></article>`).join("");document.querySelectorAll("[data-remove-download]").forEach(b=>b.onclick=()=>{d.downloads.splice(+b.dataset.removeDownload,1);Queshift.save(d);renderDownloads(d)})}
  document.querySelector("[data-reset]").addEventListener("click",()=>{if(confirm("Reset all local website settings?")){localStorage.removeItem("queshift_settings");load();notice("Settings reset.")}});
  function notice(t){const n=document.querySelector("[data-admin-notice]");n.textContent=t;setTimeout(()=>n.textContent="",2500)}function esc(v){return String(v).replace(/[&<>"']/g,c=>({"&":"&amp;","<":"&lt;",">":"&gt;",'"':"&quot;","'":"&#39;"}[c]))}
});
